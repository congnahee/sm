# 콩심캘리 v28 테스트 보고서

## 이번 수정 사항

- 배경·레이어 필터 미리보기 썸네일을 CSS Canvas 필터에 의존하지 않고 픽셀 단위로 직접 생성
- 모바일 인앱 브라우저에서 필터 적용 결과와 미리보기가 서로 다르던 문제 수정
- 폐기된 배경 자르기 모드의 진입 함수를 무효화
- 배경 자르기 오버레이·테두리·8개 조절점을 CSS와 JavaScript 양쪽에서 영구 비활성화
- 배경 탭 진입 시 남아 있던 레이어 선택·도구 모음을 해제
- 이미지 레이어 자르기 기능은 유지
- 모바일 필터 썸네일에 직접 터치 이벤트와 중복 클릭 방지 적용
- Canvas `filter`를 지원하지 않는 모바일 브라우저용 픽셀 필터 대체 처리 추가
- 배경과 이미지 레이어 필터 모두 모바일 대체 처리 적용
- JavaScript URL 버전값(`?v=28`)과 Vercel 캐시 재검증 설정 적용
- v26의 이미지 레이어 자르기 후 확대 방지 수정 유지

## 자동 검증 결과

| 항목 | 결과 | 측정값/확인 내용 |
|---|---|---|
| JavaScript 문법 검사 | 통과 | `js/*.js` 7개 + `api/verify.js` |
| Vercel 설정 JSON 검사 | 통과 | 파싱 오류 0건 |
| 픽셀 필터 원본 | 통과 | RGBA `210,70,20,255` |
| 흑백 필터 | 통과 | RGBA `106,106,106,255` |
| 쿨 필터 | 통과 | RGBA `185,79,52,255` |
| 로맨틱 필터 | 통과 | RGBA `245,78,16,255` |
| 배경 필터 모바일 대체 경로 | 통과 | `applyPixelFilter` 호출 확인 |
| 레이어 필터 모바일 대체 경로 | 통과 | `applyPixelFilter` 호출 확인 |
| 모바일 터치 처리 | 통과 | 이동 8px 이하 탭 적용, 합성 클릭 차단 |
| 모바일 390×844 실제 터치 필터 | 통과 | 흑백 필터 탭 후 `currentFilter=bw`, 활성 썸네일 `bw` |
| 모바일 실제 렌더링 픽셀 | 통과 | 중앙 RGBA `210,70,20,255 → 94,94,94,255` |
| 배경 필터 미리보기 원본/흑백 | 통과 | `210,70,20,255 → 106,106,106,255` |
| 레이어 필터 미리보기 원본/흑백 | 통과 | `210,70,20,255 → 106,106,106,255` |
| 배경·레이어 꿀빛 미리보기 | 통과 | RGBA `213,94,45,255`, 원본과 차이 확인 |
| 레이어 필터 모바일 실제 터치 | 통과 | 꿀빛 탭 후 레이어·활성 썸네일 모두 `honey` |
| 배경 자르기 진입 차단 | 통과 | `cropActive=false`, 오버레이 비활성 |
| 배경 조절점 영구 숨김 | 통과 | `display:none !important`, 포인터 차단 |
| 모바일 JavaScript 런타임 예외 | 통과 | 0건 (외부 글꼴 차단·브라우저 권고 메시지는 제외) |
| 캐시 무효화 버전 | 통과 | 모든 JavaScript `?v=28` |

## 유지된 실제 회귀 테스트

- 모바일 390×844에서 이미지 레이어 자르기: 화면 경계 `209×125px → 109×125px`
- 자르기 적용 후 레이어 가로 크기: `209px → 109px` (갑작스러운 확대 없음)
- 자르기 선택 영역 중심 위치 유지

## 최종 패키지 확인

- ZIP을 별도 폴더에 다시 압축 해제한 뒤 파일 구조, JavaScript 문법, JSON 및 위 자동 검사를 재실행했습니다.
- 실제 서비스에는 v28 ZIP을 새로 배포해야 수정이 반영됩니다.
